defmodule Emisint.Repo.Migrations.AddMdeSchoolIndexResult do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:mde_school_index_results, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :school_year, :text, null: false
      add :overall_index, :decimal
      add :growth_index, :decimal
      add :proficiency_index, :decimal
      add :graduation_index, :decimal
      add :el_progress_index, :decimal
      add :school_quality_index, :decimal
      add :subject_participation_index, :decimal
      add :el_participation_index, :decimal
      add :support_category_name, :text
      add :support_category_reason, :text

      add :created_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :mde_building_id,
          references(:mde_buildings,
            column: :id,
            name: "mde_school_index_results_mde_building_id_fkey",
            type: :uuid,
            prefix: "public"
          ), null: false
    end

    create index(:mde_school_index_results, [:mde_building_id, :school_year])

    create unique_index(:mde_school_index_results, [:mde_building_id, :school_year],
             name: "mde_school_index_results_unique_building_year_index"
           )
  end

  def down do
    drop_if_exists unique_index(:mde_school_index_results, [:mde_building_id, :school_year],
                     name: "mde_school_index_results_unique_building_year_index"
                   )

    drop constraint(:mde_school_index_results, "mde_school_index_results_mde_building_id_fkey")

    drop_if_exists index(:mde_school_index_results, [:mde_building_id, :school_year])

    drop table(:mde_school_index_results)
  end
end
